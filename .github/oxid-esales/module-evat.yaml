# {{ $ids := "oevattbe" }}ids: {{ print $ids }}
# {{ $org := "oxid-esales" }}organisation: {{ print $org }}
# {{ $name := "evat-module" }}name: {{ print $name }}
# {{ $order := "01" }}order: {{ print $order }}
prepare_shop:
  copy_script_targets: |
    vendor/oxid-esales/evat-module/tests/scripts
  git:
    shop_ref: '{{ .Data.global.git.default_ref }}'
    custom_ini:
      xdebug: 'xdebug.mode=debug,profile,coverage'

install_module:
  matrix:
    testplan: '["-"]'
  title: '{{ print $name }}-{{ print $order }}'
  cache:
    prefix: &install_module_prefix 'moduleInstallation-ce-{{ .Github.SHA }}-{{ .Github.RunID }}'
  ids: &ids '{{ print $ids }}'
  activate: *ids
  git:
    module:
      url: &git_module_url '{{ .Github.Repository }}'
      ref: '{{ .Github.RefName }}'
  package_name: &package_name '{{ print $org }}/{{ print $name }}'
  path: *ids

phpunit:
  matrix:
    testplan: '["~/defaults/module_phpunit_unit.yaml","~/defaults/module_phpunit_integration.yaml"]'
  load_shop: *install_module_prefix
  custom_script: |
    cat >source/vendor/oxid-esales/evat-module/tests/scripts/unit_failure_pattern.txt <<EOF
    fail
    \\.\\=\\=
    Warning
    Notice
    Fatal
    error
    Error
    DID NOT FINISH
    Test file ".+" not found
    Cannot open file
    No tests executed
    Could not read
    Warnings: [1-9][0-9]*
    Errors: [1-9][0-9]*
    Failed: [1-9][0-9]*
    Risky: [1-9][0-9]*
    EOF
    
    cp source/vendor/oxid-esales/evat-module/tests/scripts/unit_failure_pattern.txt source/vendor/oxid-esales/evat-module/tests/scripts/integration_failure_pattern.txt

codeception:
  matrix:
    testplan: 'skip'
  title: '{{ print $name }}-{{ print $order }}'
  load_shop: *install_module_prefix

runtest:
  matrix:
    testplan: 'skip'
  title: '{{ print $name }}-{{ print $order }}'
  load_shop: *install_module_prefix

sonarcloud:
  matrix:
    testplan: '["-"]'
  title: '{{ print $name }}-{{ print $order }}'
  project_key: 'OXID-eSales_vat_tbe_services'
  project_name: *package_name
  parameters: |
    -Dsonar.language=php
    -Dsonar.scm.provider=git
    -Dsonar.sources=src
    -Dsonar.tests=tests

phpcs_tests:
  skip: true

styles:
  skip: true

finish:
  slack_title: 'Module {{ print $name }} ({{ .Github.RefName }}) on {{ .Github.Repository }} by {{ .Github.Actor }}'
